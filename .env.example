# Required for the live Bes chat (Settings → Chat). Without it the chat falls
# back to a canned Taglish response library and says so in the UI.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PISO_MODEL=claude-opus-5
PORT=8787
